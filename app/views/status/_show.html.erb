<div id="status" class="<%= controller_name %> <%= action_name %>">

  <div id="update">
    <div class="container">
      <%= render 'layouts/messages' %>
      <div class="row">
        <div class="col-md-12">
          <div class="current-status">
            <span class="updated">
              You are at 30% of the Free Plan of 1 Gb
            </span>
            <div class="status">
              <div class="color green"></div>
              Billing Information
            </div>
            <div class="help">
              <% if user.customer_id %>
    This account is billed to: <%= user.last_4_digits %>
                <a data-toggle="modal" href="#card-data" class="btn btn-mini" type="button">Update card</a>
              <% else %>
                <a data-toggle="modal" href="#card-data" class="btn btn-mini" type="button">Add card</a>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          <div class="panel panel-default modules">
            <!-- List group -->
            <ul class="list-group">
              <li class="list-group-item">
                Data Integration
                <div class="status">Operational</div>
              </li>
              <li class="list-group-item">
                WorkSpace
                <div class="status down">Down</div>
              </li>
              <li class="list-group-item">
                            StoreFront
                <div class="status">Operational</div>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

<div>

<%= render 'shared/update_card', user: user %>
